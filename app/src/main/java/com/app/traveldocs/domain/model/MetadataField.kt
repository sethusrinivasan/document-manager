package com.app.traveldocs.domain.model

enum class MetadataField {
    ID_NUMBER,
    HOLDER_NAME,
    EXPIRY_DATE,
    ISSUE_DATE,
    BOOKING_REFERENCE,
    FLIGHT_DETAILS,
    HOTEL_NAME,
    POLICY_NUMBER,
    COVERAGE_PERIOD,
    DESTINATION,
    VISA_NUMBER
}
